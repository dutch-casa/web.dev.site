// Sorting Exercise
//
// Implement these sorting algorithms:
//
// 1. bubbleSort(arr) - Sort array using bubble sort
//    - Compare adjacent elements and swap if out of order
//    - Repeat until no swaps needed
//    - Return a new sorted array (don't modify original)
//
// 2. insertionSort(arr) - Sort array using insertion sort
//    - Build sorted portion one element at a time
//    - Insert each element into its correct position
//    - Return a new sorted array
//
// 3. sortByKey(arr, key) - Sort array of objects by a specific key
//    - Return a new sorted array
//    - Example: sortByKey([{name: "B"}, {name: "A"}], "name") -> [{name: "A"}, {name: "B"}]

function bubbleSort(arr) {
  // Your code here
}

function insertionSort(arr) {
  // Your code here
}

function sortByKey(arr, key) {
  // Your code here
}

module.exports = { bubbleSort, insertionSort, sortByKey };
