// Searching Exercise
//
// Implement these search algorithms:
//
// 1. linearSearch(arr, target) - Find target in array using linear search
//    - Return the index of target, or -1 if not found
//    - Works on unsorted arrays
//
// 2. binarySearch(arr, target) - Find target in SORTED array using binary search
//    - Return the index of target, or -1 if not found
//    - Array is guaranteed to be sorted in ascending order
//
// 3. findFirst(arr, predicate) - Find first element matching a condition
//    - predicate is a function that returns true/false
//    - Return the first element where predicate(element) is true
//    - Return undefined if no match found

function linearSearch(arr, target) {
  // Your code here
}

function binarySearch(arr, target) {
  // Your code here
}

function findFirst(arr, predicate) {
  // Your code here
}

module.exports = { linearSearch, binarySearch, findFirst };
