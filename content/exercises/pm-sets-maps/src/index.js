// Sets and Maps Exercise
//
// Complete the following functions using Set and Map:
//
// 1. uniqueValues(arr) - Return array with duplicates removed (use Set)
//    Example: [1, 2, 2, 3] -> [1, 2, 3]
//
// 2. wordFrequency(str) - Count frequency of each word (use Map)
//    Return a Map where keys are words, values are counts
//    Example: "a b a" -> Map { "a" => 2, "b" => 1 }
//
// 3. intersection(arr1, arr2) - Find common elements (use Set)
//    Example: [1, 2, 3], [2, 3, 4] -> [2, 3]
//
// 4. groupBy(arr, keyFn) - Group array elements by a key function (use Map)
//    Example: groupBy([1, 2, 3, 4], n => n % 2 === 0 ? "even" : "odd")
//    -> Map { "odd" => [1, 3], "even" => [2, 4] }

function uniqueValues(arr) {
  // Your code here
}

function wordFrequency(str) {
  // Your code here
}

function intersection(arr1, arr2) {
  // Your code here
}

function groupBy(arr, keyFn) {
  // Your code here
}

module.exports = { uniqueValues, wordFrequency, intersection, groupBy };
