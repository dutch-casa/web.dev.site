// Classes Exercise
//
// Create the following classes:
//
// 1. Counter class:
//    - Constructor takes optional initial value (default 0)
//    - increment() - increase count by 1
//    - decrement() - decrease count by 1
//    - reset() - reset to initial value
//    - getCount() - return current count
//
// 2. BankAccount class:
//    - Constructor takes owner name and initial balance (default 0)
//    - deposit(amount) - add to balance, return new balance
//    - withdraw(amount) - subtract from balance if sufficient funds
//      - Return new balance if successful
//      - Throw Error "Insufficient funds" if not enough balance
//    - getBalance() - return current balance
//    - Static method createJointAccount(account1, account2)
//      - Return new account with combined balances and "Joint" as owner

class Counter {
  // Your code here
}

class BankAccount {
  // Your code here
}

module.exports = { Counter, BankAccount };
